"use client";
import { useQuery, useQueryClient } from "@tanstack/react-query"; import { useEffect } from "react"; import { createClient } from "@/lib/supabase/client"; import { getCustomerOrders, getCustomerQuotations, getCustomerRequestStatus } from "@/services/customer/customer-service";
export function useCustomerQuotations(requestId?: string) { const queryClient = useQueryClient(); const query = useQuery({ queryKey: ["customer-quotations", requestId ?? "latest"], queryFn: () => getCustomerQuotations(requestId), refetchOnWindowFocus: true, staleTime: 0 }); useEffect(() => { const supabase = createClient(); const channel = supabase.channel(`customer-quotes:${requestId ?? "latest"}`).on("postgres_changes", { event: "*", schema: "public", table: "quotations" }, () => void queryClient.invalidateQueries({ queryKey: ["customer-quotations"] })).on("postgres_changes", { event: "*", schema: "public", table: "quote_requests", filter: requestId ? `id=eq.${requestId}` : undefined }, () => void queryClient.invalidateQueries({ queryKey: ["customer-quotations"] })).on("postgres_changes", { event: "*", schema: "public", table: "orders" }, () => void queryClient.invalidateQueries({ queryKey: ["customer-orders"] })).subscribe(); return () => { void supabase.removeChannel(channel); }; }, [queryClient, requestId]); return query; }
export function useCustomerOrders() { const queryClient = useQueryClient(); const query = useQuery({ queryKey: ["customer-orders"], queryFn: getCustomerOrders }); useEffect(() => { const channel = createClient().channel("customer-orders").on("postgres_changes", { event: "*", schema: "public", table: "orders" }, () => void queryClient.invalidateQueries({ queryKey: ["customer-orders"] })).subscribe(); return () => { void createClient().removeChannel(channel); }; }, [queryClient]); return query; }
export function useCustomerRequestStatus(requestId?: string) { const queryClient = useQueryClient(); const query = useQuery({ queryKey: ["customer-request-status", requestId], queryFn: () => getCustomerRequestStatus(requestId as string), enabled: Boolean(requestId), staleTime: 0 }); useEffect(() => { if (!requestId) return; const supabase = createClient(); const channel = supabase.channel(`customer-request-status:${requestId}`).on("postgres_changes", { event: "UPDATE", schema: "public", table: "quote_requests", filter: `id=eq.${requestId}` }, () => void queryClient.invalidateQueries({ queryKey: ["customer-request-status", requestId] })).subscribe(); return () => { void supabase.removeChannel(channel); }; }, [queryClient, requestId]); return query; }
