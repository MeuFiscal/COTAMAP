"use client";
import { BusinessShell } from "@/features/business/components/business-shell";
import Link from "next/link"; import { useQuery } from "@tanstack/react-query"; import { getBusinessDashboard } from "@/services/analytics/dashboard-service";
export default function BusinessDashboardPage() { const query = useQuery({ queryKey: ["business-dashboard"], queryFn: getBusinessDashboard, staleTime: 30_000 }); return <BusinessShell><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-[#F97316]">Visão geral</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">Dashboard da empresa</h1><p className="mt-2 text-black/55">Comece o expediente com os principais indicadores da operação.</p></div>{query.data?.generatedAt ? <time className="text-xs text-black/45" dateTime={query.data.generatedAt}>Atualizado às {new Date(query.data.generatedAt).toLocaleTimeString("pt-BR", { hour: "2-digit", minute: "2-digit" })}</time> : null}</header><div className="mt-5"><Link href="/empresa/operador" className="inline-flex rounded-xl border border-[#F97316]/30 px-4 py-2 text-sm font-black text-[#F97316]">Mudar usuário</Link></div>{query.isLoading ? <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4" aria-label="Carregando indicadores">{Array.from({ length: 8 }, (_, index) => <div key={index} className="h-28 animate-pulse rounded-2xl bg-white/70" />)}</div> : query.error ? <p role="alert" className="mt-8 rounded-2xl bg-red-50 p-6 text-red-700">Não foi possível carregar o dashboard. Tente novamente.</p> : <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{query.data?.metrics.map((metric) => <article key={metric.label} className="rounded-2xl bg-white p-5 shadow-sm transition hover:-translate-y-0.5"><p className="text-sm text-black/55">{metric.label}</p><strong className="mt-2 block text-2xl">{metric.value}</strong></article>)}</div>}</BusinessShell>; }
