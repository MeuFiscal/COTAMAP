"use client";
import { BusinessShell } from "@/features/business/components/business-shell"; import { useQuery } from "@tanstack/react-query"; import { getBusinessDashboard } from "@/services/analytics/dashboard-service";
export default function BusinessDashboardPage() { const query = useQuery({ queryKey: ["business-dashboard"], queryFn: getBusinessDashboard }); return <BusinessShell><h1 className="text-4xl font-black">Dashboard da empresa</h1><p className="mt-2 text-black/55">Indicadores calculados diretamente dos dados disponíveis.</p>{query.isLoading ? <p className="mt-8">Calculando métricas...</p> : query.error ? <p role="alert" className="mt-8 text-red-600">Não foi possível carregar o dashboard.</p> : <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{query.data?.metrics.map((metric) => <article key={metric.label} className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm text-black/55">{metric.label}</p><strong className="mt-2 block text-2xl">{metric.value}</strong></article>)}</div>}</BusinessShell>; }
